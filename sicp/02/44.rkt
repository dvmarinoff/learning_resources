#lang racket
;; ex 02.44
;; picture language up-split procedure
;;
;; Define the procedure up-split used by corner-split. It is
;; similar to right- split, except that it switches the roles of
;; below and beside.

(define (main n) n)
