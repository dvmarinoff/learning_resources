#lang racket
;; ex 02.41
;; ordered pairs of distinct positive integers
;;
;; Write a procedure to find all ordered triples of distinct
;; positive integers i, j, and k less than or equal to a given
;; integer n that sum to a given integer s.

(define (main n) n)
